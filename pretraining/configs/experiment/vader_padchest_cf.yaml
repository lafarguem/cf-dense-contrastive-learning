# @package _global_
defaults:
  - /evaluator/visualizations
  - /evaluator/kmeans_clustering
  - /evaluator/latent_metrics
  - override /data/dataset@data.train: padchest
  - override /data/dataset@data.eval: padchest
  - override /model: single_branch_unet
  - override /loss/dense: vader
  - override /loss/instance: simclr
  - override /evaluator/reducer@evaluator.visualizations.reducer: pca

loss:
  global_loss_weight: 0
  dense:
    sampler:
      name: random_overlap
      num_samples: 1500

train:
  exp_name: vader_padchest


data:
  eval:
    dataset:
      cf_suffixes: ['sc', 'pe', 'sc_pe']
      cache: false
      max_cache_size: 5
    dataloader:
      batch_size: 8
  train:
    dataloader:
      batch_size: 8