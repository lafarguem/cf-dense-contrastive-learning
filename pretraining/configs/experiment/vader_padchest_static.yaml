# @package _global_
defaults:
  - /evaluator/visualizations
  - /evaluator/kmeans_clustering
  - /evaluator/latent_metrics
  - override /data/dataset@data.train: padchest
  - override /data/dataset@data.eval: padchest
  - override /data/augmentation@data.train.augmentation: static
  - override /data/augmentation@data.eval.augmentation: static
  - override /model: single_branch_unet
  - override /loss/dense: vader_static
  - override /loss/instance: simclr
  - override /evaluator/reducer@evaluator.visualizations.reducer: umap

loss:
  global_loss_weight: 0
  dense:
    sampler:
      name: stride
      stride: 6

train:
  exp_name: vader_padchest_static


data:
  eval:
    dataset:
      cf_suffixes: []
      cache: false
      max_cache_size: 5
    dataloader:
      batch_size: 8
  train:
    dataloader:
      batch_size: 8